# =============================================================================
# TheSimGov - Environment Variables Template for Vercel Deployment
# =============================================================================
# Copy this file to .env.local for local development
# For Vercel: Add these in Project Settings → Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED - Core Application
# -----------------------------------------------------------------------------

# MongoDB connection string (MongoDB Atlas recommended for Vercel)
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/thesimgov?retryWrites=true&w=majority

# NextAuth.js secret for JWT signing (generate with: openssl rand -base64 32)
# NextAuth v5 prefers AUTH_SECRET but NEXTAUTH_SECRET also works
AUTH_SECRET=your-super-secret-key-at-least-32-characters-long
# NEXTAUTH_SECRET=your-super-secret-key-at-least-32-characters-long  # Alternative

# -----------------------------------------------------------------------------
# REQUIRED FOR PRODUCTION - URLs
# -----------------------------------------------------------------------------

# Your production domain (set in Vercel env vars)
# Example: https://thesimgov.com or https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=https://your-domain.com

# NextAuth URL - Should match your production domain
NEXTAUTH_URL=https://your-domain.com

# -----------------------------------------------------------------------------
# OPTIONAL - Email (Password Reset)
# -----------------------------------------------------------------------------

# Resend API key for sending emails (https://resend.com)
# Leave empty to log password reset links to console
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# From address for emails (must be verified in Resend)
RESEND_FROM=noreply@your-domain.com

# -----------------------------------------------------------------------------
# OPTIONAL - Development
# -----------------------------------------------------------------------------

# Node environment (automatically set by Vercel)
NODE_ENV=development

# Enable API response validation (slows performance)
VALIDATE_RESPONSES=false

# -----------------------------------------------------------------------------
# AUTO-SET BY VERCEL (Do NOT set manually)
# -----------------------------------------------------------------------------
# VERCEL=1
# VERCEL_URL=your-app-xxxxx.vercel.app
# VERCEL_ENV=production|preview|development

# -----------------------------------------------------------------------------
# NOTES FOR VERCEL SETUP
# -----------------------------------------------------------------------------
# 
# 1. Go to your Vercel project → Settings → Environment Variables
# 
# 2. Add these REQUIRED variables:
#    - MONGODB_URI (Production, Preview, Development)
#    - AUTH_SECRET (Production, Preview, Development)
#    - NEXT_PUBLIC_APP_URL (Production only - your custom domain)
#    - NEXTAUTH_URL (Production only - your custom domain)
#
# 3. Add these OPTIONAL variables if using email:
#    - RESEND_API_KEY
#    - RESEND_FROM
#
# 4. Generate AUTH_SECRET:
#    - Run: openssl rand -base64 32
#    - Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# 5. MongoDB Atlas Setup:
#    - Create a free cluster at mongodb.com
#    - Add 0.0.0.0/0 to IP Access List (for Vercel serverless)
#    - Create a database user with read/write permissions
#    - Get connection string from Connect → Drivers
#
# =============================================================================
