# Completion Report — FID-20251127-001

**Date:** 2025-11-27

## Summary
Implemented chat MVP components and server unread infrastructure per plan:

- ChatPanel: unread badge, unread-summary handler, precise read-marking.
- MessageList: scroll detection and `onViewedEnd` callback.
- DmSelector: canonical DM rooms with per-room unread badges.
- socketInit: emits `chat:system` unread summary on `/chat` connect; integrates unread and read-mark updates.

## Files
- MOD `src/components/realtime/ChatPanel.tsx`
- MOD `src/components/realtime/MessageList.tsx`
- ADD `src/components/realtime/DmSelector.tsx`
- MOD `src/components/realtime/index.ts`
- MOD `src/realtime/socketInit.ts`

## Acceptance Criteria
- DM selector opens `dm:{uidSmall_uidLarge}` rooms and requests history.
- Unread counts show and update via events; read marks only after bottom viewed.
- Login-time unread summary emitted as `chat:system` event.

## Notes
No unapproved mounts added. Components are ready for integration at the designated UI container.

---
Auto-generated per ECHO v1.3.1.